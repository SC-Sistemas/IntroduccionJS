export const sumar = (n1, n2) => {
  return n1 + n2;
};
export const restar = (n1, n2) => {
  return n1 - n2;
};

//Export Default
//Solamante puede a ver uno por archivo, se puede importar y nombrar como uno quiera

const multiplicar = (n1, n2) => {
  return n1 * n2;
};
export default multiplicar;

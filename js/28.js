//Selectores
// Seleccionas un elemento y le das atributos, guardar y etc
const heading = document.querySelector(".heading");
console.log(heading);
console.log(heading.textContent);
console.log(heading.id);
//Eso se trata, de modificar el dom, poder manipularlo

//Aqui seleccionamos todo lo que tiene la clase
const enlaces = document.querySelectorAll(".navegacion a");
console.log(enlaces);

//Solamente se trae un elemento
// const enlaces = document.querySelector(".navegacion a");
// console.log(enlaces);
